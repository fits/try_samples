%a(href='/') back

%form.post(action='#{action}' method='post')
  Book Title:
  %input(name='post[title]' type='text')
  ISBN:
  %input(name='post[isbn]' type='text')
  %input(type='submit' value='Add')

%ul
  - books.each do |b|
    %li
      & #{b.title} - #{b.isbn}
